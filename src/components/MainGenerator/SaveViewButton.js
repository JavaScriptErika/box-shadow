import React, { Component } from 'react';

function SaveViewButton() {
    return (
        <div className="App">
            <button>Save</button>
        </div>
    );
}

export default SaveViewButton;